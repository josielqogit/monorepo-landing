// import { toast } from 'react-toastify';

// const notify = (type) => (msg) => {
//   toast[type](msg, {
//     position: "bottom-center",
//     autoClose: 5000,
//     hideProgressBar: false,
//     closeOnClick: true,
//     pauseOnHover: true,
//     draggable: true,
//     progress: undefined,
//   });
// }

// const notifyMsg = _ => next => action => {
//   if (action.type === `toast/${action.payload.type || "error"}`) {
//     notify(action.payload.type || "error")(action.payload.message || "Ops. Ocorreu um erro!");
//     next(action);
//     return;
//   }
//   next(action);
// }

export default 123;